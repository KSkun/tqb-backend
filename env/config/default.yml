app:
  addr: ":3000"
  prefix: "/api/v1"
  upload_dir: "upload"
mail:
  addr: "*email address*" # 需填写
  password: "*email password*" # 需填写
  smtp_host: "*smtp server address*" # 需填写
  smtp_port: 465
redis:
  addr: "localhost:6379"
  password: ""
  db: 0
db:
  addr: "localhost:27017"
  db: "tqb-backend"
  username: ""
  password: ""
jwt:
  secret: "*jwt secret*" # 需填写

logConf:
  log_path: "log"
  log_file_name: "tqb-backend.log"

debug: true # 调试模式开关
